# CASMI 2026 × Flyte 2: starter kit

Your brief is in [CLASS.md](CLASS.md). Read it first.

```bash
uv sync
```
```bash
uv run pytest
```

Use the Flyte config (`.flyte/config.yaml`) of your own cluster. Everything under `casmi_flyte/`
and `snippets/` is provided; the pipeline is yours to build.
